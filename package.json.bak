{
  "name": "sherin-avatar-desktop",
  "version": "0.1.3",
  "build": {
    "appId": "ai.sherin.avatar.desktop",
    "productName": "Sherin Avatar Desktop",
    "icon": "build/icon.ico",
    "artifactName": "Sherin-Avatar-Desktop-Setup-${version}.exe",
    "win": { "target": "nsis" },
    "nsis": { "oneClick": true, "perMachine": false }
  },
  "scripts": {
    "clean": "rimraf dist",
    "dist": "npm run clean && electron-builder --win"
  },
  "devDependencies": { "rimraf": "^6.0.0" }
}
const path = require("C:\Users\rafee\Downloads\sherin-avatar-desktop");
const { BrowserWindow, app } = require("electron");

function createWindow() {
  const iconPath = path.join(__dirname, "build", "icon.ico");
  const win = new BrowserWindow({ width: 1200, height: 800, icon: iconPath });
  // loadURL(...) or loadFile(...)
}
app.whenReady().then(createWindow);
